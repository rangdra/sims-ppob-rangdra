export interface IService {
  service_code: string;
  service_icon: string;
  service_name: string;
  service_tariff: number;
}

export interface IBanner {
  banner_name: string;
  banner_image: string;
  description: string;
}
